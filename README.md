# Flight-Delay-Prediction-model
Flight delay prediction using BTS airline stats and NOAA weather. End‑to‑end pipeline: merge and clean data, engineer delay_15 target, train leakage‑free Random Forest, evaluate with ROC‑AUC, and present everything in an interactive Streamlit dashboard. Run with: streamlit run src/app.py.
